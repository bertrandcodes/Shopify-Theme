{% include 'page-header', title: 'About Us' %}

<section class="about section">
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <img class="img-fluid" src="{{settings.about_img | img_url: '550x550'}}">
            </div>
            <div class="col-md-6">
                {{page.content}}
            </div>
        </div>
    </div>
</section>